# Quicker 动作集成改进方案

## 1. 目标与当前基础

目标是在 Windows 资源管理器中选中文件后，通过 Quicker 完成：

1. 将文件追加到打印助手当前任务；
2. 使用指定收藏、打印机或打印配置直接打印；
3. 无论软件是否已经运行，都只操作同一个主实例；
4. 对失败、冲突和忙碌状态给出明确结果，不出现“命令执行了但没有反应”。

当前代码已经具备可复用基础：

- `src-tauri/src/ingress.rs` 能从启动参数解析文件和文件夹；
- `src-tauri/src/lib.rs` 已启用单实例插件，第二次启动会唤醒现有窗口并发送 `files-added`；
- `src/App.tsx` 会把外部文件追加到队列，并复用重复文件检测和撤销机制；
- 安装版可执行文件名为 `PrintAssist.exe`。

因此，**当前版本已经可以通过命令行追加文件，但不能通过参数直接打印或指定配置**。

当前可用命令示例：

```text
"C:\完整安装路径\PrintAssist.exe" "D:\文档\甲.pdf" "D:\文档\乙.docx"
```

程序未运行时会启动；已经运行时会激活现有窗口并追加文件。文件夹参数也会被递归展开。现有实现会忽略 `-` 开头的参数，因此附加 `--print` 目前不会生效。

## 2. 软件侧需要强化的内容

### P1：建立正式的外部调用协议

新增统一的 `ExternalRequestV1`，替换仅包含路径的 `files-added` 事件：

```ts
interface ExternalRequestV1 {
  version: 1;
  requestId: string;
  action: 'add' | 'print';
  paths: string[];
  favoriteId?: string;
  printerName?: string;
  profileId?: string;
  duplicatePolicy?: 'ask' | 'skip' | 'include';
  busyPolicy?: 'reject' | 'enqueue';
  activateWindow?: boolean;
  confirmBeforePrint?: boolean;
}
```

建议命令格式：

```text
PrintAssist.exe --action add -- "D:\文档\甲.pdf" "D:\文档\乙.docx"

PrintAssist.exe --action print --favorite-id "fav_xxx" --duplicate skip -- "D:\文档\甲.pdf"

PrintAssist.exe --request-file "C:\Temp\printassist-request.json"
```

要求：

- 保留原来的纯路径调用方式，默认等价于 `--action add`；
- 使用成熟的命令行解析库，不手写字符串拆分；
- `--` 之后全部视为文件路径，支持空格、中文和 Unicode；
- 未识别参数、缺少参数和非法枚举必须报错，不能静默忽略；
- 初次启动和单实例转交必须使用同一套解析函数。

涉及文件：`src-tauri/src/ingress.rs`、`src-tauri/src/lib.rs`、`src/api/nativeBridge.ts`、`src/App.tsx`。

### P1：增加 JSON 请求文件模式

多选文件数量较大时，不建议把所有路径拼到命令行中。新增 `--request-file`：

```json
{
  "version": 1,
  "requestId": "由 Quicker 生成的 GUID",
  "action": "print",
  "paths": [
    "D:\\文档\\甲.pdf",
    "D:\\文档\\乙.docx"
  ],
  "favoriteId": "fav_xxx",
  "duplicatePolicy": "skip",
  "busyPolicy": "reject",
  "activateWindow": true,
  "confirmBeforePrint": false
}
```

软件读取后应立即复制请求内容并关闭文件句柄。Quicker 可在短暂延迟后删除临时文件。限制请求文件大小、路径数量及单个路径长度，并拒绝未知版本。

### P1：把“直接打印”实现为受控事务

处理顺序固定为：

1. 校验请求结构并去除请求内的完全重复路径；
2. 展开文件夹、验证文件存在且格式受支持；
3. 解析收藏、打印机和配置；
4. 按策略处理与当前列表重复的文件；
5. 以一次可撤销操作追加任务、切换打印机并加载配置；
6. 等待打印机列表和持久化配置加载完成；
7. 复用现有 `buildBatchPayload()` 校验；
8. 校验全部通过后调用现有打印流程。

配置优先级固定为：**命令显式指定 > 收藏中的打印机/配置 > 当前主界面设置**。不允许部分加载配置后继续打印。

直接打印的安全规则：

- 推荐 Quicker 的“直接打印”动作必须绑定一个收藏，确保输出配置可预期；
- 找不到收藏、打印机或配置，打印机离线，配置不兼容时终止，不回退到其他打印机；
- 没有有效文件时不得启动空打印；
- 默认只打印本次请求新增的文件，不顺带打印列表中原有的待打印文件；
- `confirmBeforePrint=true` 时显示文件数、打印机、配置和份数的确认摘要；
- 即使不确认，也显示可取消的短暂状态提示；
- 外部请求永远不能绕过现有页码、配置兼容性和打印可用性检查。

### P1：定义忙碌及批次状态

- 正在打印：`reject` 立即拒绝；`enqueue` 仅排队请求，当前批次结束后再次校验并执行；
- 已暂停：不自动恢复当前批次；新请求按忙碌策略处理；
- 当前批次已完成：外部请求自动执行“开始新批次”，并将该动作与追加文件合并成一次事务；
- 存在重复文件且策略为 `ask`：激活窗口并显示现有重复文件对话框；
- 请求按照到达顺序串行执行，禁止两个 Quicker 动作并发启动两个打印批次。

### P1：提供调用结果和防重复机制

每个请求必须包含 `requestId`。应用在短时间窗口内记录已处理 ID，避免 Quicker 重试导致重复打印。

建议支持可选的 `resultFile` 或本机命名管道，返回：

```json
{
  "requestId": "...",
  "status": "accepted|completed|rejected|failed",
  "addedCount": 2,
  "skippedCount": 0,
  "message": "已加入打印队列"
}
```

MVP 可先只在软件内提示；但“直接打印”正式开放前，应至少能够让 Quicker 判断请求是已接受还是已拒绝。不要依赖 GUI 进程的 stdout，因为第二实例把请求转交后即可退出，不能代表主实例打印成功。

### P2：增加面向 Quicker 的界面入口

在设置或帮助中增加“外部动作 / Quicker 集成”：

- 显示当前 `PrintAssist.exe` 的实际路径并可复制；
- 提供“复制添加命令”；
- 每个收藏提供“复制 Quicker 直接打印命令”；
- 显示收藏稳定 ID，重命名收藏不应导致动作失效；
- 提供“测试外部调用”按钮和最近请求日志；
- 说明直接打印使用的文件范围、重复策略及忙碌策略。

不建议要求用户手写打印机名称或配置 ID。应由软件生成命令，减少引号和标识错误。

### P2：可观测性和限制

- 记录请求时间、来源、动作、文件数量及失败阶段，但不长期记录敏感完整路径；
- 单次请求建议最多 500 个文件，与收藏和队列上限保持一致；
- 文件夹递归应沿用当前深度限制，并显示因格式不支持而跳过的数量；
- 外部请求仅接受本机文件路径，不接受 URL、UNC 凭据或任意 shell 命令；
- 程序不需要管理员权限，Quicker 也不应以管理员身份启动它，以免形成不同权限级别的实例。

### P1：增加“系统右键菜单”设置

当前 `src-tauri/windows/installer-hooks.nsh` 已在安装时写入下面两类经典菜单：

- 文件：`HKCU\Software\Classes\*\shell\PrintAssist`；
- 文件夹：`HKCU\Software\Classes\Directory\shell\PrintAssist`。

但它们目前是安装时固定启用，应用内不能检测、关闭或修复，而且命令仍是 `PrintAssist.exe "%1"`。应在“设置 → 外部集成”增加：

- `启用资源管理器右键菜单`总开关；
- 注册范围：`支持的文件`、`文件夹`，默认两者都启用；
- 菜单结构预览；
- `检查并修复`按钮；
- 当前注册状态：正常、未注册、路径失效、版本不兼容；
- 说明 Windows 11 经典菜单可能位于“显示更多选项”。

推荐菜单结构：

```text
打印助手  >  添加到打印列表
              使用默认收藏直接打印
              打开打印助手
```

其中“直接打印”仅在用户已经指定一个“右键直接打印收藏”后显示；未指定时不注册该子项，避免使用不可预期的当前配置打印。

实现要求：

1. Rust 后端新增 `get_shell_integration_status`、`register_shell_integration`、`unregister_shell_integration`；
2. 只写当前用户的 `HKCU`，不申请管理员权限；
3. 仅创建和删除本软件拥有的 `PrintAssist` 注册表键，不触碰其他软件菜单；
4. 注册命令必须使用当前运行 EXE 的绝对路径并完整加引号；
5. 升级或安装路径变化时，根据保存的开关自动修复旧路径；
6. 卸载程序始终清理这些键；卸载后重装应以新安装状态为准；
7. 设置操作失败时恢复开关状态并显示具体错误，不能出现界面显示已启用但注册失败；
8. 安装脚本和应用内注册逻辑共用相同的键名与命令协议，避免两套定义漂移；
9. 将注册表实际状态作为显示依据，本地设置只保存用户偏好；
10. 增加注册、重复注册、路径修复、注销和卸载清理测试。

第一阶段可以继续使用经典静态 Shell verb，开发量小且可靠。正式命令应更新为：

```text
"C:\...\PrintAssist.exe" --action add -- "%1"
```

但经典 `%1` 方式不适合可靠传递大量多选文件，尤其不能直接用多次单文件启动来触发“直接打印”。批量直接打印应等待 `IExplorerCommand`/`IDropTarget` 处理器，或者先通过 Quicker 的完整文件列表调用。

第二阶段如要求在 Windows 11 第一层新式右键菜单中显示，应实现轻量原生 `IExplorerCommand` COM 组件，并通过 MSIX 或 sparse package 提供应用身份。组件只负责快速取得完整选择列表并把结构化请求交给主程序，不在 Explorer 进程中做文件扫描、配置读取或打印。这样还可以可靠支持多选和子菜单。该阶段应独立实施，不能为了一个设置开关立即引入复杂 Shell 扩展。

#### 便携版注册策略

便携版可以注册经典右键菜单。仍写入当前用户的 `HKCU`，命令指向便携版当前 `PrintAssist.exe` 的绝对路径，不需要管理员权限。但必须明确注册不会随 EXE 自动移动：目录变化、移动盘盘符变化或介质未连接时，旧菜单都会失效。

便携模式下设置项调整为：

- `注册到当前位置`：持久写入当前 EXE 路径；
- `启动时检查并修复路径`：检测同名注册键指向旧位置时，征得用户偏好后更新；
- `退出时自动注销`：适合 U 盘或临时使用，默认推荐开启；
- `立即注销`：删除本软件拥有的全部经典菜单键；
- 显示警告：移动便携版前应先注销，移动后可重新注册。

实现细节：

1. 复用项目现有的便携模式检测，不另建第二套判断；
2. 注册时保存 EXE 规范化绝对路径、版本及便携标识；
3. 每次启动只检查本软件键，不自动接管来源不明的同名键；
4. EXE 位于可移动磁盘时，默认建议“退出时自动注销”；
5. 异常退出可能来不及注销，因此下次启动仍要提供失效项检查和清理；
6. 便携版不建议实现依赖 sparse package 的 Windows 11 第一层菜单，因为包注册会在系统中留下安装状态，与便携预期冲突；默认使用“显示更多选项”中的经典菜单；
7. 便携版升级覆盖原文件时注册可保持；更换目录后必须修复；
8. 测试固定磁盘、U 盘盘符变化、路径含中文/空格、文件被移走及异常退出场景。

## 3. Quicker 动作编写方式

Quicker 官方的“运行或打开”模块可以运行 EXE 并附加命令行参数；“获取选择的文件（夹）”模块可以取得资源管理器当前选择的完整路径列表。因此可以直接实现。

### 动作一：添加到打印助手（当前版本即可使用）

1. 新建组合动作，关联到“文件操作”，扩展名限制为软件支持的 PDF、图片及 Office/WPS 文档；
2. 添加“获取选择的文件（夹）”步骤，输出保存为列表变量 `selectedPaths`；
3. 若列表为空，提示“请先选择文件”并停止；
4. 将列表中的每个路径转换为英文双引号包裹的参数，再用空格连接；
5. 添加“运行或打开”步骤：
   - 路径或命令：浏览选择实际安装目录下的 `PrintAssist.exe`；
   - 参数：上一步生成的路径参数文本；
   - 不勾选“以管理员身份运行”；
   - 不等待进程结束。

最终参数形态必须类似：

```text
"D:\资料\文件 1.pdf" "D:\资料\文件 2.docx"
```

不能只用换行连接，也不能遗漏每个路径各自的双引号。此动作会追加文件并激活打印助手，但不会自动打印。

### 动作二：直接打印（软件完成协议后）

少量文件可使用：

```text
--action print --favorite-id "fav_xxx" --duplicate skip --busy reject -- "D:\资料\文件 1.pdf"
```

推荐正式动作使用 JSON 请求文件：

1. 获取选中文件列表；
2. 生成 GUID 作为 `requestId`；
3. 通过 JSON 序列化模块生成请求，不能用普通文本替换手工转义路径；
4. 将 JSON 以 UTF-8 写入 Quicker 临时目录；
5. 运行：

```text
"C:\完整安装路径\PrintAssist.exe" --request-file "C:\临时目录\request.json"
```

6. 若实现了结果文件，则等待结果并根据状态显示“已接受 / 已拒绝 / 打印失败”；否则只提示“请求已发送”，实际结果以打印助手为准；
7. 确认软件已读取后删除临时请求文件。

### 动作菜单建议

一个 Quicker 动作可设置右键子菜单：

- 左键：添加到打印列表；
- 右键菜单“使用默认收藏直接打印”；
- “选择收藏后打印”；
- “添加并打开打印助手”；
- “仅添加，不切换当前打印机配置”。

首版不建议让 Quicker 自己维护打印机和配置清单。由打印助手管理收藏并生成稳定命令，Quicker 只负责传递文件和操作意图。

## 4. 验收标准

- 程序关闭和已运行两种状态下，单选、多选、含空格及中文路径均能追加；
- 连续快速触发两个动作仍只有一个应用实例，请求按顺序处理；
- 直接打印只打印本次请求文件，并准确使用指定收藏；
- 收藏重命名后，使用收藏 ID 的 Quicker 动作继续有效；
- 打印机离线、配置损坏、路径失效、格式不支持时不会误用默认打印机；
- 同一 `requestId` 重试不会重复追加或重复打印；
- 100 个以上文件通过请求文件模式可靠传递；
- 正在打印、暂停和已完成批次三种状态均符合定义；
- 原有资源管理器右键菜单、SendTo、拖放和纯路径启动方式保持兼容。

## 5. 推荐实施顺序

1. **命令协议阶段**：建立 `ExternalRequestV1`、统一初次启动与单实例解析，保留旧纯路径兼容。
2. **经典右键菜单阶段**：增加应用内注册、检测、修复和注销，先可靠支持“添加到打印列表”。
3. **可靠传输阶段**：实现 `--request-file`、请求 ID 去重、参数校验及错误提示。
4. **添加动作阶段**：接入现有追加队列流程，补齐已完成批次和忙碌状态测试；此时即可发布 Quicker“添加”动作。
5. **直接打印阶段**：接入收藏解析、配置预检和“仅打印本次请求文件”，实现严格失败策略。
6. **反馈与易用性阶段**：增加结果回传、外部调用设置页、复制 Quicker 命令及最近请求日志。
7. **Windows 11 原生菜单（可选）**：仅在确需第一层菜单和可靠大批量多选时实施 `IExplorerCommand` 与包身份。
8. **联调阶段**：在 Windows 安装版上覆盖中文路径、大批量文件、重复触发、离线打印机和软件冷热启动场景。
