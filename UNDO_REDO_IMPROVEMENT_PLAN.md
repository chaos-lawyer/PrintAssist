# 全面撤销与重做功能改进方案

## 目标

在主工作台增加统一的 `Ctrl+Z` 撤销能力，覆盖队列编辑和打印参数修改；同时提供 `Ctrl+Y` / `Ctrl+Shift+Z` 重做。清空或移除队列文件后立即执行，不再弹确认框，用户可通过快捷键、按钮或提示条恢复。

“撤销”只针对尚未提交到外部系统的可逆编辑。已经发送到打印机的任务、文件系统操作及无法可靠恢复的持久化删除不得伪装成可撤销。

## 最新代码现状

- `queueReducer.ts` 已集中处理追加、移除、清空、排序、移动、克隆、粘贴、单文件配置和批量配置，适合作为队列快照恢复入口。
- `globalSettings`、`selectedRowKeys`、`activeId` 仍由 `App.tsx` 独立管理，不能只给 `queueReducer` 增加 history。
- `handleBatchRemove` 和 `handleClearQueue` 仍使用 `Modal.confirm`。
- “开始新批次/恢复上一批”使用 `previousBatchBackupRef + canRestoreBatch`，与未来撤销栈职责重复。
- 快捷键已有统一 guard；输入框中的 `Ctrl+Z` 必须继续交给浏览器完成文字撤销。

## 撤销范围

### 必须支持

| 操作 | 撤销结果 |
|---|---|
| 添加文件、文件夹、拖入或从历史重新加载 | 移除本次新增项，恢复原选择和活动行 |
| 单项/批量移除 | 按原 ID、顺序、状态和配置恢复文件 |
| 清空列表 | 完整恢复队列、顺序、选择和活动行 |
| 拖动、Alt+方向键、剪切后粘贴 | 恢复移动前顺序 |
| 复制粘贴、克隆 | 移除本次生成项 |
| 文件名等列排序 | 恢复原顺序和排序模式 |
| 单文件设置 | 恢复该文件原 override |
| 批量设置 | 一次恢复所有目标文件的原 override |
| 全局打印设置 | 恢复颜色、单双面、翻转、纸盘、缩放、份数、页码、多页拼接等 |
| 应用保存的打印机配置（含数字键） | 恢复应用前的全局设置和配置标识 |
| 仅保留失败项 | 恢复被移除的成功项和原完成状态 |
| 开始新批次 | 恢复上一批；现有“恢复上一批”按钮改为调用同一个 undo |

### 不进入撤销栈

- 选择/取消选择、展开行、打开弹窗、列显示偏好等纯界面状态；
- 复制动作和尚未粘贴的剪切标记；`Esc` 继续负责取消剪切；
- 文件元数据和页数等后台分析结果；
- 开始、暂停、继续、终止及完成打印；开始打印时清空撤销/重做栈；
- 打开文件、打开文件夹、刷新打印机等外部操作；
- 保存/删除打印机配置等原生持久化操作。

打印历史删除/清空可以后续通过恢复 localStorage 记录接入，但不应与主队列第一阶段混做。打印机配置删除会删除原生持久化数据，在后端实现软删除或回收站之前必须保留确认框。

## 核心设计

### 1. 使用有界工作区快照

新增 `src/features/undo/undoTypes.ts`：

```ts
interface WorkspaceSnapshot {
  queueState: QueueState;
  globalSettings: PrintSettings;
  selectedRowKeys: string[];
  activeId: string | null;
}

interface UndoEntry {
  label: string;
  before: WorkspaceSnapshot;
  after: WorkspaceSnapshot;
  mergeKey?: string;
  createdAt: number;
}
```

快照使用明确的深拷贝，不能保存可变对象引用。撤销栈和重做栈各最多保留 50 项，仅驻留内存，不跨应用重启持久化。

使用快照比逐项编写逆操作更可靠：可以原样恢复文件 ID、顺序、状态、配置和选择，也能覆盖一次批量设置涉及多个文件的情况。

### 2. 统一事务入口

新增 `src/features/undo/useUndoHistory.ts`，提供：

- `commit(label, mutation, options?)`
- `undo()` / `redo()`
- `canUndo` / `canRedo`
- `undoLabel` / `redoLabel`
- `clearHistory()`

所有用户可撤销操作必须通过 `commit`，不得在各组件中自行 push history。`commit` 在操作前后分别取快照；状态没有实际变化时不新增记录；新操作发生后清空 redo 栈。

为 `queueReducer` 增加内部动作 `restore_snapshot`，一次恢复完整 `QueueState`。`App.tsx` 同时恢复 `globalSettings`、选择和活动行，并过滤已经不存在的选择 ID。

需要维护最新状态 ref，确保快速连续操作、异步文件选择和 React 批处理不会记录陈旧快照。异步操作只在成功取得结果并准备修改状态时提交事务。

### 3. 设置修改合并

- 单文件和批量设置仅在用户点击“保存/应用”时生成一个事务，抽屉内尚未保存的输入继续使用输入控件自己的撤销。
- 点击分段按钮、下拉项等离散全局设置，每次生成一个事务。
- 份数、页码表达式等连续输入使用 `mergeKey`，同一字段在 800ms 内的连续变化合并为一次，避免按一次 `Ctrl+Z` 只退回一个字符。
- 切换打印机及自动加载默认配置视为一个异步事务：只在最终配置稳定后记录；过期请求不得覆盖撤销后的状态。
- 应用保存配置或数字配置视为一次事务，加载失败时不写入撤销栈。

### 4. 打印边界

- `phase` 为 `printing / pausing / paused / terminating` 时禁用撤销和重做。
- 执行 `begin_print` 前清空两个栈，防止用户撤销到与真实打印输出不一致的状态。
- 打印完成结果本身不可撤销。
- 用户点击“开始新批次”时，新建一条“开始新批次”事务，因此之后可恢复刚完成的批次；现有恢复提示条继续展示，但按钮调用 `undo()`。
- 一旦在新批次中执行新的编辑，提示条消失，但普通 `Ctrl+Z` 仍按栈顺序工作。

## 交互调整

### 撤销/重做入口

- 顶栏右侧、快捷键问号入口旁增加 `Undo2` 和 `Redo2` 图标按钮。
- Tooltip 动态显示 `撤销：移除 3 个文件（Ctrl+Z）`、`重做：移除 3 个文件（Ctrl+Y）`；无记录时禁用。
- 快捷键帮助界面增加“撤销”和“重做”。
- `Ctrl+Z` 执行 workspace undo；`Ctrl+Y` 和 `Ctrl+Shift+Z` 均执行 redo。
- 焦点位于输入控件或可编辑文本时，不拦截这些组合键，让系统完成文字撤销/重做。

### 取消队列确认框

修改 `App.tsx`：

- `handleBatchRemove` 直接提交“移除 N 个文件”事务，不再调用 `Modal.confirm`；
- `handleClearQueue` 直接提交“清空 N 个文件”事务，不再调用 `Modal.confirm`；
- 单行删除、右键删除、`Delete`、批量删除和清空按钮必须最终走同一删除事务函数；
- 操作后显示短提示：`已移除 3 个文件  [撤销]` 或 `已清空 36 个文件  [撤销]`；提示按钮调用 undo；
- 提示消失不影响 `Ctrl+Z`，也不能覆盖更晚操作的撤销目标。

本阶段只取消待打印队列的移除与清空确认框。打印历史和打印机配置属于独立持久化数据，在接入相同恢复保证之前保留确认框。

## 操作标签

统一使用用户可理解的标签，供 Tooltip、提示和无障碍播报复用：

- 添加 N 个文件
- 移除 N 个文件
- 清空 N 个文件
- 移动 N 个文件
- 粘贴 N 个文件
- 修改“文件名”的设置
- 修改 N 个文件的设置
- 修改全局打印设置
- 应用配置“配置名”
- 调整文件排序
- 开始新批次

撤销或重做后使用 `aria-live="polite"` 播报，例如“已撤销：清空 36 个文件”。不要为每次普通参数调整弹出成功 Toast。

## 代码调整

### 新增

- `src/features/undo/undoTypes.ts`
- `src/features/undo/useUndoHistory.ts`
- `src/features/undo/UndoRedoControls.tsx`
- 对应单元测试和集成测试

### 修改

- `src/features/queue/queueReducer.ts`：增加 `restore_snapshot`，保持其他业务 action 纯净。
- `src/App.tsx`：建立工作区快照、统一 commit 入口、替换删除/清空确认框、接入设置事务和打印边界。
- `src/features/queue/PrintQueue.tsx`：所有删除、移动、粘贴和排序通过 App 提供的可撤销回调，避免组件内组合多个 action 形成多条记录。
- `src/features/settings/GlobalSettingsPanel.tsx`：向上报告稳定的设置变更来源/字段 key，支持合并连续输入。
- `src/features/shortcuts/shortcutRegistry.ts`、`ShortcutHelpModal.tsx`：增加撤销/重做说明。
- `src/styles.css`：增加顶栏按钮、撤销操作提示和禁用状态样式。

## 风险与约束

- 快照包含队列全部项目，大批量文件时会占用内存；限制 50 项，并在测试中覆盖 1000 个文件的连续操作。
- 后台元数据返回时不得生成撤销记录；恢复缺失元数据后允许现有分析流程重新补齐。
- 全局设置异步加载必须带请求版本号，避免撤销后旧请求再次覆盖状态。
- 连续拖动过程中只在 drop 完成时记录一次，拖动预览不能写 history。
- 撤销恢复的文件仅恢复应用中的队列引用；如果源文件已被移动或删除，后续打印仍按现有校验逻辑报错。

## 测试与验收

- 对每个“必须支持”操作验证 undo、redo、再次编辑后清空 redo。
- 验证删除/清空无确认框，Toast 撤销与 `Ctrl+Z` 结果一致。
- 验证单项/批量配置、连续份数输入、数字配置切换只生成合理数量的事务。
- 验证剪切移动、拖动多选和排序恢复原 ID 与精确顺序。
- 验证开始打印后不能撤销，打印结果不会因 undo 改写。
- 验证开始新批次后，提示按钮和 `Ctrl+Z` 恢复同一批次且不会恢复两次。
- 验证输入框内 `Ctrl+Z/Y` 只影响文字，不影响工作区。
- 验证 50 项上限、1000 文件队列、快速连续操作和异步打印机配置加载。
- 执行 `npm test`、`npm run lint`、`npm run build`。

## 推荐实施顺序

1. 实现快照、undo/redo 栈和 reducer 恢复入口，先覆盖队列增删、清空和移动。
2. 替换删除/清空确认框，增加提示中的撤销按钮和顶栏入口。
3. 接入单文件、批量和全局设置事务及连续输入合并。
4. 合并“恢复上一批”，接入排序、粘贴、仅保留失败项和保存配置切换。
5. 加入打印边界、异步竞态保护、性能测试和全量回归。
