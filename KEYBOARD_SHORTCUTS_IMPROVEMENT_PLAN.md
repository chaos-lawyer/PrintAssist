# 单键快捷键与帮助界面改进方案

## 目标

为批量打印高频操作提供无需组合键的快捷方式，并增加统一的快捷键帮助界面。快捷键必须服从当前焦点、弹窗和打印状态，避免用户输入文字或调整参数时误操作。

本次只新增快捷键入口与帮助能力，不改变已有按钮、右键菜单、拖放和打印业务逻辑。

## 最新代码现状

- `src/App.tsx` 监听 `Delete/Backspace`、`Ctrl+A` 和 `Ctrl+P`。
- `src/features/queue/PrintQueue.tsx` 监听 `Ctrl+C`、`Ctrl+V`、`Ctrl+Shift+A`、方向键、`Enter`、`Shift+F10` 等；当前 `Enter` 打开文件设置。
- 队列已有内部复制/粘贴、打开文件、定位文件、单项/批量设置等处理函数，应直接复用。
- `handleSelectSavedProfile` 已能加载当前打印机的保存配置，`savedProfiles` 的界面顺序可作为数字快捷键顺序。
- 快捷键目前分散在两个组件中，缺少统一的冲突判断和用户可见说明。

## 快捷键方案

### 单键快捷键

| 按键 | 操作 | 规则 |
|---|---|---|
| `A` | 添加文件 | 仅主界面可编辑状态生效 |
| `F` | 添加文件夹 | 仅主界面可编辑状态生效 |
| `S` | 设置所选文件 | 1 项打开单文件设置；多项打开批量设置；未选择时使用活动行 |
| `H` | 打开打印历史 | 主界面无其他弹窗时生效 |
| `C` | 复制所选文件项 | 复用队列内部剪贴板；无选择时使用活动行 |
| `X` | 剪切所选文件项 | 仅可编辑状态生效，并显示半透明“已剪切”状态 |
| `V` | 粘贴文件项 | 粘贴到活动行之后；没有活动行时追加到末尾 |
| `Enter` | 打开活动文件 | 替代当前“打开设置”行为；设置统一改用 `S` |
| `L` | 在文件夹中显示 | 只定位活动文件；多选时也只处理活动行，避免打开多个资源管理器窗口 |
| `1`–`9` | 应用打印机配置 | 对应当前打印机配置列表中的第 1–9 个保存配置 |
| `/` | 打开快捷键界面 | 无论队列是否为空均可使用，但编辑文本时不拦截 `/` |

数字键规则：

- 顺序必须与右侧“驱动配置”列表完全一致，并在列表项右侧显示数字键帽；
- 只对当前打印机的保存配置编号，“不使用已保存配置”不占编号；
- 配置不足 9 个时，其余数字无操作；超过 9 个的配置仍可鼠标选择；
- 配置不兼容或正在加载时不应用，并给出简短原因；
- 不使用小键盘键区作为独立映射，`event.key` 为 `1`–`9` 时统一处理。

### 保留和补充的常规快捷键

| 快捷键 | 操作 |
|---|---|
| `Ctrl+A` | 全选文件 |
| `Ctrl+Shift+A` | 选择活动文件的全部副本 |
| `Delete` | 移除所选文件 |
| `Ctrl+P` | 开始打印，保持现有高风险操作保护 |
| `↑` / `↓` | 移动活动行 |
| `Shift+↑` / `Shift+↓` | 扩展选择范围 |
| `Home` / `End` | 跳到首项/末项；可与 `Shift` 组合扩展选择 |
| `Alt+↑` / `Alt+↓` | 调整所选文件顺序，保持现有行为 |
| `Shift+F10` / 菜单键 | 打开右键菜单，保持现有行为 |
| `Esc` | 先关闭最上层菜单或弹窗；没有浮层时清除队列选择 |

可选增强：打印已经开始后，用 `Space` 在“暂停/继续”之间切换，但不得用空格启动打印；该项建议放到第二阶段，避免与按钮空格激活行为冲突。

## 剪切与粘贴语义

当前 `queueClipboard` 只有复制快照，需要扩展为 `{ mode: 'copy' | 'cut', snapshots, sourceIds }`：

- `C`：保存快照，可重复粘贴；
- `X`：保留原项但显示半透明状态，不立即删除，避免误操作导致文件项消失；
- 首次 `V`：将剪切项移动到目标位置，而不是复制后再生成重复项；完成后清空剪切状态；
- 按 `Esc`、开始打印、开始新批次、清空列表或源项失效时清除剪切状态；
- 打印中或批次完成后禁止 `X/V`，但允许 `C`；
- 快捷键、右键菜单中的复制/剪切/粘贴必须调用同一组函数，不能形成两套行为。

## 快捷键生效保护

新增统一的 `shouldIgnoreShortcut(event)`，满足任一条件时忽略单键快捷键：

- 焦点位于 `input`、`textarea`、`select`、可编辑元素或数值输入控件；
- Modal、Drawer、下拉菜单、右键菜单或输入法候选状态处于活动状态；
- `event.isComposing`、`event.repeat` 或 `event.defaultPrevented` 为真；
- 按下 `Ctrl`、`Alt`、`Meta`，或按下不属于该命令的 `Shift`；
- 当前窗口不在前台；
- 快捷键帮助界面已打开。

所有命令还需进行业务状态判断：

- `A/F/X/V/S` 在打印中或批次完成状态禁用；
- `S` 多选时直接打开现有批量设置，单选或活动行时打开现有文件设置；
- `Enter/L` 要求存在有效活动行；
- 数字键要求打印机已选择、配置兼容且当前没有配置加载任务；
- 被状态阻止时只给出一次简短提示，按键长按不得连续弹消息。

## 快捷键帮助界面

新增 `src/features/shortcuts/ShortcutHelpModal.tsx`，入口为：

1. 键盘 `/`；
2. 顶栏右上角、打印历史按钮旁新增 `CircleHelp` 问号图标按钮，Tooltip 为“快捷键（/）”。

界面建议：

- 标题“快捷键”；宽度约 640–720px，不使用新页面；
- 顶部提供“启用单键快捷键”开关，默认开启，并说明“输入文字或打开弹窗时自动停用”；
- 按“文件与队列”“设置与配置”“打印控制”“导航与选择”四组展示；
- 每行左侧为操作名称和必要说明，右侧使用统一键帽展示按键；
- `1–9` 配置区同时显示当前打印机前 9 个配置名称及兼容状态；切换打印机后实时更新；
- 底部提供“恢复默认快捷键”，首版不开放任意改键，避免引入冲突录入、布局差异和迁移问题。

设置持久化到 `localStorage`，例如 `printassist_shortcuts_v1`，仅保存是否启用单键快捷键。现有组合键始终有效，不受该开关影响。

## 代码调整

### 新增

- `src/features/shortcuts/shortcutRegistry.ts`：集中定义命令、默认按键、分组、说明和状态要求。
- `src/features/shortcuts/shortcutGuards.ts`：统一焦点、输入法、浮层和重复按键判断。
- `src/features/shortcuts/ShortcutHelpModal.tsx`：帮助与开关界面。
- 对应单元测试文件。

### 修改

- `src/App.tsx`：整合全局命令 `A/F/S/H/1–9//`，增加帮助弹窗状态和顶栏问号入口。
- `src/features/queue/PrintQueue.tsx`：将现有监听重构为队列命令 `C/X/V/Enter/L`，补充剪切模式，并让右键菜单复用命令函数。
- `src/features/settings/GlobalSettingsPanel.tsx`：为前 9 个保存配置显示数字键帽。
- `src/components/AppContextMenu.tsx`：将菜单快捷键标签由 `Ctrl+C/V` 更新为实际启用的 `C/X/V/Enter/L`。
- `src/styles.css`：增加键帽、帮助界面、顶栏问号按钮和剪切行状态样式。

不要新增第三方快捷键库；现有事件规模较小，注册表加统一 guard 更容易控制 Windows/WebView2 的焦点和输入法边界。

## 测试与验收

- 输入中文、搜索历史、输入份数、重命名配置时，所有单键均不得触发业务操作；
- 逐一验证无选择、单选、多选、打印中、批次完成及弹窗打开状态；
- 验证 `S` 单选/多选分流，`Enter` 打开文件，`L` 只打开活动文件夹；
- 验证复制可多次粘贴、剪切只移动一次、取消剪切和跨状态清理；
- 验证数字键顺序与当前打印机配置列表一致，切换打印机后立即更新；
- 验证 `/` 与问号按钮打开同一界面，关闭后焦点返回原位置；
- 验证主键盘及小键盘数字、中文输入法、Caps Lock、100%/125%/150% Windows 缩放；
- 执行 `npm test`、`npm run lint`、`npm run build`。

## 推荐实施顺序

1. 建立快捷键注册表、统一 guard 和帮助界面，迁移现有组合键但不改变行为。
2. 实现 `A/F/S/H/Enter/L//`，处理当前 `Enter` 行为迁移。
3. 实现 `C/X/V` 及剪切状态，并同步右键菜单。
4. 接入 `1–9` 配置选择和配置列表键帽。
5. 完成状态矩阵、输入法、Windows 缩放与全量回归测试，再评估是否加入空格暂停/继续。
