# 右键菜单一体化改进方案

## 目标

将无意义的 WebView2 默认右键菜单替换为与当前界面一致、随操作对象变化的应用菜单。右键只提供高频且安全的快捷操作；已有按钮和快捷键继续保留，不改变打印行为。

## 最新代码现状

- `src/features/queue/PrintQueue.tsx` 仅在表头使用 `Dropdown trigger={['contextMenu']}`，用于显示/隐藏列；文件行和空白区域仍无应用菜单。
- 队列已有打开文件、在文件夹中显示、单项设置、复制/粘贴文件项、选择重复项和移除等能力，可直接复用，不应重复实现业务逻辑。
- `src/features/settings/PrinterProfileManagerModal.tsx` 已有 fixed + portal 的自绘菜单，但仅由“更多”按钮打开，配置卡片右键仍未接入。
- `src/features/history/PrintHistoryModal.tsx` 已有展开、收藏、重新加载和删除操作，但没有行级右键入口。
- `src/styles.css` 已定义颜色、圆角、阴影及 `profile-context-menu` 样式，可作为统一菜单的基础。

## 设计原则

1. **按对象显示操作**：文件行、历史记录、配置卡片和表头使用不同菜单；没有有效操作的普通背景不显示空菜单。
2. **保留文本编辑菜单**：`input`、`textarea` 和可编辑区域继续使用系统剪切/复制/粘贴菜单。
3. **与当前选择一致**：右键已选行时操作全部选中项；右键未选行时先将其设为唯一选中项，再打开菜单。
4. **状态决定可用性**：打印中或批次完成后，设置、粘贴、移除等修改性操作禁用并显示原因；打开文件和定位文件仍可用。
5. **危险操作不隐藏确认**：删除历史、移除多项等继续使用现有确认流程；菜单只作为入口。

## 实施方案

### P1：建立统一菜单基础

新增 `src/components/AppContextMenu.tsx`，从现有配置菜单提取 fixed + `createPortal` 实现，不引入新依赖。

组件统一负责：

- 根据鼠标坐标或键盘目标位置打开，并限制在可视区域内；
- 图标、文字、可选快捷键、分组线、禁用态和危险态；
- 点击外部、滚动、窗口失焦或 `Escape` 时关闭；
- 打开后聚焦第一项，支持上下键、Home/End、Enter/Space；
- `Shift+F10` 和键盘菜单键可打开当前对象菜单；
- 菜单项执行前先关闭菜单，异步失败继续使用现有 `message.error`。

在应用根节点增加兜底 `onContextMenu`：事件未被业务菜单处理，且目标不是文本编辑控件时，仅阻止 WebView2 默认菜单。不要在 `document` 上无条件禁用右键。

统一样式放入 `src/styles.css`：菜单宽度约 180–220px、单项高度 30–32px、14px Lucide 图标、12–13px 字号，沿用现有 `--color-*`、`--radius-*` 和 `--shadow-card`。移除 `profile-context-menu*` 的重复样式后由公共类替代。

### P1：打印队列菜单

修改 `src/features/queue/PrintQueue.tsx`，把当前键盘快捷键与菜单共用的逻辑提取为 `copySelection`、`pasteAfterTarget`、`selectDuplicates` 等函数，避免两套行为。

文件行菜单：

| 操作 | 单选 | 多选 | 锁定时 |
|---|---:|---:|---|
| 打开文件 | 显示 | 隐藏 | 可用 |
| 在文件夹中显示 | 显示 | 隐藏 | 可用 |
| 文件打印设置 | 显示 | 隐藏 | 禁用 |
| 批量设置（N 项） | 隐藏 | 显示 | 禁用 |
| 选择此文件的全部副本 | 显示 | 隐藏 | 可用 |
| 复制文件项（N 项） `Ctrl+C` | 显示 | 显示 | 可用 |
| 粘贴到此项之后 `Ctrl+V` | 有内部剪贴板时显示 | 有内部剪贴板时显示 | 禁用 |
| 移除（N 项） `Delete` | 显示 | 显示 | 禁用、危险色 |

需要由 `App.tsx` 向 `PrintQueue` 补充批量设置和批量移除回调，直接复用现有 `setIsBatchSettingsOpen(true)` 与 `handleBatchRemove`。

空队列/表格空白区域只提供“选择文件”和“粘贴文件项”（剪贴板非空时）；不要复制完整行菜单。

表头保留现有列显示菜单，并补充“恢复默认列”。排序仍由左键点击表头完成，避免同一菜单混入过多排序命令。表头菜单也迁移到统一组件和样式。

### P2：打印历史菜单

修改 `src/features/history/PrintHistoryModal.tsx`：

- 行菜单提供“展开/收起详情”“重新加载此批文件”“收藏/取消收藏”“删除记录”；
- 删除项放在分组末尾并使用危险色，仍调用现有确认框；
- 右键行不触发展开；双击或现有文件名单击行为保持不变；
- 展开的单文件明细可提供“打开文件”和“在文件夹中显示”，复用 `openFile`、`showInFolder`，无有效路径时禁用。

### P2：打印机配置菜单

修改 `src/features/settings/PrinterProfileManagerModal.tsx`：

- 配置卡片增加 `onContextMenu`，菜单内容沿用“重命名、复制配置、导出配置、删除配置”；
- “更多”按钮和右键共同调用同一打开函数；
- 用公共组件替换当前局部 `MenuState`、关闭监听和菜单 DOM；
- 正在编辑名称或执行异步操作时禁用冲突项，不关闭编辑输入框的系统文本菜单。

### P3：其他区域的处理

- 设置面板、标题栏、按钮和普通空白区没有额外业务操作，不创建形式化的空菜单，只屏蔽无意义的浏览器菜单。
- 输入框保留系统文本菜单；链接、按钮仍以左键和键盘为主，不复制同名右键命令。
- 后续新增可右键对象必须复用 `AppContextMenu`，不得再新增独立浮层实现。

## 测试与验收

- 公共组件测试：边界定位、外部点击/Escape 关闭、禁用项、危险项、键盘导航和焦点恢复。
- 队列测试：单选/多选语义、右键未选行后选择更新、打印中禁用修改、复制粘贴复用现有逻辑。
- 历史与配置测试：菜单命令调用现有处理函数，删除仍出现确认框，“更多”按钮与右键内容一致。
- 人工在 Windows WebView2 验证 100%/125%/150% 缩放、屏幕四角定位、滚动关闭、长文件名和弹窗层级。
- 执行 `npm test`、`npm run lint`、`npm run build`。

验收结果应满足：非编辑区域不再出现浏览器默认菜单；所有应用菜单视觉一致、位置不越界；键盘和右键均可使用；任何现有按钮、快捷键及打印流程没有行为变化。

## 推荐实施顺序

1. 实现公共菜单和根节点默认菜单兜底，先完成队列行菜单。
2. 统一表头菜单，并接入队列空白区域。
3. 迁移打印历史和打印机配置菜单。
4. 完成 Windows 缩放、键盘可访问性与全量回归测试。
